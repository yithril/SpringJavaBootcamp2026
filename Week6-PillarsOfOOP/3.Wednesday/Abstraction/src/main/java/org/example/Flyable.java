package org.example;

public interface Flyable {
    //all methods in an interface are public
    //similar to an abstract method, we just write the signature
    //anything that IMPLEMENTS this interface MUST use the method
    void fly();
}
